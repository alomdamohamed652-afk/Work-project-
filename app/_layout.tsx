import {Stack,usePathname} from "expo-router";import {SafeAreaProvider} from "react-native-safe-area-context";import {View} from "react-native";import BottomNav from "@/components/BottomNav";export default function RootLayout(){const path=usePathname();const customer=(path.startsWith("/customer")&&!path.startsWith("/customer/cart"));return <SafeAreaProvider><View style={{flex:1}}><Stack screenOptions={{headerShown:false,animation:"slide_from_right",contentStyle:{backgroundColor:"#F7F7F7"}}}/>{customer&&!path.includes("/cart")?<BottomNav role="customer"/>:null}</View></SafeAreaProvider>}