import {Stack,router,usePathname} from "expo-router";import {SafeAreaProvider} from "react-native-safe-area-context";import {useEffect} from "react";import {Platform,View} from "react-native";import AsyncStorage from "@react-native-async-storage/async-storage";import Constants from "expo-constants";import BottomNav from "@/components/BottomNav";const API=(process.env.EXPO_PUBLIC_API_URL||"").replace(/\/$/,"");export default function RootLayout(){const path=usePathname();useEffect(()=>{let mounted=true;let sub:any;let responseSub:any;const setup=async()=>{try{const Notifications=await import("expo-notifications");Notifications.setNotificationHandler({handleNotification:async()=>({shouldPlaySound:true,shouldSetBadge:true,shouldShowBanner:true,shouldShowList:true})});if(Platform.OS==="android")await Notifications.setNotificationChannelAsync("default",{name:"وصّلني",importance:Notifications.AndroidImportance.HIGH,sound:"default",vibrationPattern:[0,250,250,250],showBadge:true});if(Platform.OS!=="web"&&API){const token=await AsyncStorage.getItem("auth_token");if(token){const permission=await Notifications.getPermissionsAsync();let status=permission.status;if(status!=="granted"&&status!=="denied"){const requested=await Notifications.requestPermissionsAsync();status=requested.status}if(status==="granted"){const projectId=Constants.expoConfig?.extra?.eas?.projectId||Constants.easConfig?.projectId;if(projectId){const push=(await Notifications.getExpoPushTokenAsync({projectId})).data;if(push&&mounted){const old=await AsyncStorage.getItem("registered_push_token");if(old!==push){const r=await fetch(API+"/api/notifications/push-token",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`},body:JSON.stringify({token:push,platform:Platform.OS})});if(r.ok)await AsyncStorage.setItem("registered_push_token",push)}}}}}}sub=Notifications.addNotificationReceivedListener(()=>{});responseSub=Notifications.addNotificationResponseReceivedListener(response=>{const orderId=response.notification.request.content.data?.orderId;if(orderId)router.push({pathname:"/customer/tracking",params:{orderId:String(orderId)}})});}catch{}};setup();return()=>{mounted=false;sub?.remove?.();responseSub?.remove?.()};},[]);const customer=(path.startsWith("/customer")&&!path.startsWith("/customer/cart"));return <SafeAreaProvider><View style={{flex:1}}><Stack screenOptions={{headerShown:false,animation:"slide_from_right",contentStyle:{backgroundColor:"#F7F7F7"}}}/>{customer&&!path.includes("/cart")?<BottomNav role="customer"/>:null}</View></SafeAreaProvider>}
