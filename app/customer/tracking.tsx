import AsyncStorage from "@react-native-async-storage/async-storage";
import { useEffect,useState } from "react";
import { StyleSheet,Text,View } from "react-native";
import MapView,{Marker,Region} from "react-native-maps";
import { SafeAreaView } from "react-native-safe-area-context";
import { theme } from "@/constants/theme";
const API_URL=(process.env.EXPO_PUBLIC_API_URL||"").replace(/\/$/,"");
export default function CustomerTracking(){const [driver,setDriver]=useState<any>(null),[region,setRegion]=useState<Region>({latitude:30.0444,longitude:31.2357,latitudeDelta:.3,longitudeDelta:.3});useEffect(()=>{let alive=true;const load=async()=>{const t=await AsyncStorage.getItem("auth_token"),r=await fetch(API_URL+"/api/location/my-driver",{headers:{Authorization:"Bearer "+t}}),d=await r.json(),first=(d.drivers||[]).find((x:any)=>x.latitude!=null&&x.longitude!=null);if(alive&&first){setDriver(first);setRegion(x=>({...x,latitude:Number(first.latitude),longitude:Number(first.longitude)}))}};load();const id=setInterval(load,5000);return()=>{alive=false;clearInterval(id)}},[]);return <SafeAreaView style={styles.safe}><View style={styles.page}><Text style={styles.title}>تتبع الدليفري</Text><Text style={styles.subtitle}>{driver?"المندوب: "+driver.full_name+" • تحديث كل 5 ثواني":"مفيش مندوب متصل حاليًا"}</Text><MapView style={styles.map} region={region}>{driver&&<Marker coordinate={{latitude:Number(driver.latitude),longitude:Number(driver.longitude)}} title={driver.full_name} description="موقع مباشر"/>}</MapView></View></SafeAreaView>}
const styles=StyleSheet.create({safe:{flex:1,backgroundColor:theme.background},page:{padding:20,flex:1},title:{fontSize:29,fontWeight:"900",color:theme.text,textAlign:"right"},subtitle:{fontSize:12,color:theme.muted,textAlign:"right",marginTop:5,marginBottom:14},map:{flex:1,minHeight:500,borderRadius:20}});