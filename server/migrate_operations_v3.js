const{pool}=require('./db');
async function main(){await pool.query(`
UPDATE membership_tiers SET promotion_orders=CASE WHEN name='الفضية' THEN 30 ELSE promotion_orders END,upgrade_orders=CASE WHEN name='الفضية' THEN 30 ELSE upgrade_orders END,retention_orders=CASE WHEN name='الذهبية' THEN 20 ELSE retention_orders END,maintenance_orders=CASE WHEN name='الذهبية' THEN 20 ELSE maintenance_orders END;
ALTER TABLE reward_definitions ADD COLUMN IF NOT EXISTS min_order_amount NUMERIC(12,2) NOT NULL DEFAULT 0;ALTER TABLE reward_definitions ADD COLUMN IF NOT EXISTS max_discount_amount NUMERIC(12,2);ALTER TABLE reward_definitions ADD COLUMN IF NOT EXISTS usage_limit INTEGER;ALTER TABLE reward_definitions ADD COLUMN IF NOT EXISTS budget_amount NUMERIC(12,2);ALTER TABLE reward_definitions ADD COLUMN IF NOT EXISTS starts_at TIMESTAMPTZ;ALTER TABLE reward_definitions ADD COLUMN IF NOT EXISTS expires_at TIMESTAMPTZ;
CREATE TABLE IF NOT EXISTS reward_restaurants(reward_id UUID NOT NULL REFERENCES reward_definitions(id) ON DELETE CASCADE,restaurant_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,PRIMARY KEY(reward_id,restaurant_id));CREATE TABLE IF NOT EXISTS reward_customers(reward_id UUID NOT NULL REFERENCES reward_definitions(id) ON DELETE CASCADE,customer_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,PRIMARY KEY(reward_id,customer_id));CREATE TABLE IF NOT EXISTS reward_usages(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),reward_id UUID REFERENCES reward_definitions(id) ON DELETE SET NULL,customer_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,order_id UUID REFERENCES orders(id) ON DELETE SET NULL,discount_amount NUMERIC(12,2) NOT NULL DEFAULT 0,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS reward_usage_customer_idx ON reward_usages(customer_id,reward_id,created_at DESC);
CREATE OR REPLACE FUNCTION assign_order_office() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.office_id IS NULL AND NEW.delivery_latitude IS NOT NULL AND NEW.delivery_longitude IS NOT NULL THEN SELECT osa.office_id INTO NEW.office_id FROM office_service_areas osa WHERE osa.is_active=true AND osa.center_latitude IS NOT NULL AND osa.center_longitude IS NOT NULL AND osa.radius_meters IS NOT NULL AND (6371000*2*asin(sqrt(power(sin(radians(NEW.delivery_latitude-osa.center_latitude)/2),2)+cos(radians(osa.center_latitude))*cos(radians(NEW.delivery_latitude))*power(sin(radians(NEW.delivery_longitude-osa.center_longitude)/2),2))))<=osa.radius_meters ORDER BY osa.radius_meters ASC LIMIT 1; END IF; RETURN NEW; END $$;DROP TRIGGER IF EXISTS trg_assign_order_office ON orders;CREATE TRIGGER trg_assign_order_office BEFORE INSERT ON orders FOR EACH ROW EXECUTE FUNCTION assign_order_office();
`);console.log('Operations v3 migration completed')}module.exports=main;if(require.main===module)main().then(()=>pool.end()).catch(async e=>{console.error(e);await pool.end();process.exit(1)});