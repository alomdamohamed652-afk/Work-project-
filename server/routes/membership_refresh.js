const express=require('express');const router=express.Router();const{pool}=require('../db');const{requireAuth,requireRole}=require('../auth');router.get('/',requireAuth,requireRole('customer'),async(req,res,next)=>{try{await pool.query('SELECT sync_monthly_membership($1)',[req.user.id]);const{rows}=await pool.query(`SELECT cm.*,mt.name,mt.sort_order,mt.maintenance_orders,mt.upgrade_orders,mt.badge_label,mt.badge_image_url,mt.reward_config FROM customer_memberships cm LEFT JOIN membership_tiers mt ON mt.id=cm.tier_id WHERE cm.user_id=$1`,[req.user.id]);res.json({membership:rows[0]||null})}catch(e){next(e)}});module.exports=router;