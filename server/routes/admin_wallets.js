const express=require("express");const router=express.Router();const {pool}=require("../db");const {requireAuth,requireRole}=require("../auth");router.use(requireAuth,requireRole("admin","staff"));router.get("/",async(req,res,next)=>{try{const {rows}=await pool.query(`SELECT u.id,u.full_name,u.phone,COALESCE(w.balance,0) balance,COALESCE((SELECT COUNT(*) FROM wallet_transactions t WHERE t.user_id=u.id),0)::int transactions_count FROM users u LEFT JOIN customer_wallets w ON w.user_id=u.id WHERE u.role='customer' ORDER BY u.full_name LIMIT 500`);res.json({customers:rows})}catch(e){next(e)}});module.exports=router;
