const express=require("express");const router=express.Router();const{pool}=require("../db");const{bcrypt,signToken,requireAuth}=require("../auth");function normalizePhone(v){return String(v||"").replace(/[\s-]/g,"")}function validPhone(v){return /^01\d{9}$/.test(v)}function primary(){const p=normalizePhone(process.env.PRIMARY_ADMIN_PHONE);return validPhone(p)?p:null}function publicUser(u){const x={...u};delete x.password_hash;return x}
router.post("/register-customer",async(req,res)=>{try{const b=req.body||{},fullName=String(b.fullName||"").trim(),phone=normalizePhone(b.phone),secondaryPhone=normalizePhone(b.secondaryPhone),email=b.email?String(b.email).trim().toLowerCase():null,password=String(b.password||""),confirmPassword=String(b.confirmPassword||"");if(fullName.length<2)return res.status(400).json({error:"اكتب الاسم بالكامل"});if(!validPhone(phone))return res.status(400).json({error:"رقم الهاتف غير صحيح"});if(secondaryPhone&&!validPhone(secondaryPhone))return res.status(400).json({error:"رقم الهاتف الاحتياطي غير صحيح"});if(secondaryPhone===phone&&secondaryPhone)return res.status(400).json({error:"رقم الهاتف الاحتياطي يجب أن يكون مختلفًا"});if(password.length<8)return res.status(400).json({error:"كلمة المرور يجب أن تكون 8 أحرف على الأقل"});if(password!==confirmPassword)return res.status(400).json({error:"تأكيد كلمة المرور غير مطابق"});const hash=await bcrypt.hash(password,12);const{rows}=await pool.query(`INSERT INTO users(full_name,phone,secondary_phone,email,password_hash,role,area,address,building,floor,apartment,address_notes,governorate,center,password_set_at) VALUES($1,$2,$3,$4,$5,'customer',$6,$7,$8,$9,$10,$11,$12,$13,now()) RETURNING id,full_name,phone,secondary_phone,email,role,status,area,address,building,floor,apartment,address_notes,governorate,center,avatar_url,created_at,updated_at,password_set_at`,[fullName,phone,secondaryPhone||null,email,hash,b.area?String(b.area).trim():null,b.address?String(b.address).trim():null,b.building?String(b.building).trim():null,b.floor?String(b.floor).trim():null,b.apartment?String(b.apartment).trim():null,b.addressNotes?String(b.addressNotes).trim():null,b.governorate?String(b.governorate).trim():null,b.center?String(b.center).trim():null]);const user=rows[0];res.status(201).json({user,token:signToken(user),onboardingRequired:true})}catch(e){if(e.code==="23505")return res.status(409).json({error:"رقم الهاتف أو البريد الإلكتروني مستخدم بالفعل"});console.error(e);res.status(500).json({error:"تعذر إنشاء الحساب"})}});
router.post("/continue",async(req,res)=>{try{const phone=normalizePhone(req.body?.phone);if(!validPhone(phone))return res.status(400).json({error:"رقم الهاتف غير صحيح"});const p=primary(),isAdmin=Boolean(p&&phone===p);const{rows}=await pool.query("SELECT id,full_name,phone,secondary_phone,email,password_hash,role,status,area,address,building,floor,apartment,address_notes,governorate,center,avatar_url,created_at,updated_at,password_set_at FROM users WHERE phone=$1 LIMIT 1",[phone]);let u=rows[0];if(!u)return res.status(404).json({error:"الحساب غير موجود. اختر إنشاء حساب جديد."});if(!isAdmin&&u.password_set_at)return res.status(400).json({error:"هذا الحساب مؤمّن بكلمة مرور. استخدم تسجيل الدخول العادي."});if(isAdmin&&u.role!=="admin"&&u.role!=="super_admin"){const r=await pool.query("UPDATE users SET role='admin',status='active',updated_at=now() WHERE id=$1 RETURNING id,full_name,phone,secondary_phone,email,password_hash,role,status,area,address,building,floor,apartment,address_notes,governorate,center,avatar_url,created_at,updated_at,password_set_at",[u.id]);u=r.rows[0]}if(u.status!=="active")return res.status(403).json({error:"الحساب غير متاح حاليًا"});res.json({user:publicUser(u),token:signToken(u),legacy:true})}catch(e){console.error(e);res.status(500).json({error:"تعذر تسجيل الدخول"})}});
router.post("/login",async(req,res)=>{try{const identifier=String(req.body?.identifier||"").trim(),password=String(req.body?.password||"");if(!identifier||!password)return res.status(400).json({error:"اكتب رقم الهاتف وكلمة المرور"});const{rows}=await pool.query("SELECT id,full_name,phone,email,password_hash,role,status,area,address,building,floor,apartment,address_notes,governorate,center,avatar_url,password_set_at FROM users WHERE lower(coalesce(email,''))=$1 OR phone=$2 LIMIT 1",[identifier.toLowerCase(),normalizePhone(identifier)]);const u=rows[0];if(!u||u.status!=="active"||!u.password_hash||!await bcrypt.compare(password,u.password_hash))return res.status(401).json({error:"بيانات الدخول غير صحيحة"});res.json({user:publicUser(u),token:signToken(u)})}catch(e){console.error(e);res.status(500).json({error:"تعذر تسجيل الدخول"})}});
router.get("/me",requireAuth,(req,res)=>res.json({user:req.user}));router.patch("/profile",requireAuth,async(req,res)=>{try{const b=req.body||{},fullName=String(b.fullName||"").trim(),email=b.email?String(b.email).trim().toLowerCase():null;if(fullName.length<2)return res.status(400).json({error:"الاسم غير صحيح"});const{rows}=await pool.query("UPDATE users SET full_name=$1,email=$2,avatar_url=COALESCE($3,avatar_url),updated_at=now() WHERE id=$4 RETURNING id,full_name,phone,email,role,status,area,address,building,floor,apartment,address_notes,governorate,center,avatar_url,created_at,updated_at",[fullName,email,b.avatarUrl||null,req.user.id]);res.json({user:rows[0],token:signToken(rows[0])})}catch(e){if(e.code==="23505")return res.status(409).json({error:"البريد الإلكتروني مستخدم بالفعل"});res.status(500).json({error:"تعذر حفظ الحساب"})}});router.patch("/password",requireAuth,async(req,res)=>{try{const current=String(req.body?.currentPassword||""),password=String(req.body?.password||""),confirm=String(req.body?.confirmPassword||"");const{rows}=await pool.query("SELECT password_hash,password_set_at FROM users WHERE id=$1",[req.user.id]);if(!rows[0])return res.status(404).json({error:"الحساب غير موجود"});if(rows[0].password_set_at&&(!current||!await bcrypt.compare(current,rows[0].password_hash)))return res.status(400).json({error:"كلمة المرور الحالية غير صحيحة"});if(password.length<8)return res.status(400).json({error:"كلمة المرور الجديدة 8 أحرف على الأقل"});if(password!==confirm)return res.status(400).json({error:"تأكيد كلمة المرور غير مطابق"});await pool.query("UPDATE users SET password_hash=$1,password_set_at=now(),updated_at=now() WHERE id=$2",[await bcrypt.hash(password,12),req.user.id]);res.json({ok:true})}catch(e){console.error(e);res.status(500).json({error:"تعذر تغيير كلمة المرور"})}});module.exports=router;
