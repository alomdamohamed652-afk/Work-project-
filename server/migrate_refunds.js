const{pool}=require('./db');
async function main(){await pool.query(`CREATE TABLE IF NOT EXISTS refund_requests(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),checkout_id UUID REFERENCES checkout_sessions(id) ON DELETE SET NULL,order_id UUID REFERENCES orders(id) ON DELETE SET NULL,customer_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,amount NUMERIC(12,2) NOT NULL CHECK(amount>0),method TEXT NOT NULL CHECK(method IN ('wallet','vodafone_cash','instapay','cash')),status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending','completed','rejected')),reason TEXT,created_by UUID REFERENCES users(id) ON DELETE SET NULL,processed_by UUID REFERENCES users(id) ON DELETE SET NULL,processed_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT now(),updated_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS refund_requests_customer_idx ON refund_requests(customer_id,created_at DESC);CREATE INDEX IF NOT EXISTS refund_requests_status_idx ON refund_requests(status,created_at DESC);CREATE UNIQUE INDEX IF NOT EXISTS refund_requests_order_unique_idx ON refund_requests(order_id) WHERE order_id IS NOT NULL AND status IN ('pending','completed');`);console.log('Refund migration completed')};module.exports=main;if(require.main===module)main().then(()=>pool.end()).catch(async e=>{console.error('Refund migration failed:',e);await pool.end();process.exit(1)});