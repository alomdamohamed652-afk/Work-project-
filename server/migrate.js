const {pool}=require("./db");async function main(){await pool.query(`CREATE EXTENSION IF NOT EXISTS pgcrypto;CREATE TABLE IF NOT EXISTS users(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),full_name TEXT NOT NULL,phone TEXT UNIQUE,secondary_phone TEXT,email TEXT UNIQUE,password_hash TEXT NOT NULL,role TEXT NOT NULL DEFAULT 'customer' CHECK(role IN ('customer','driver','restaurant','staff','admin')),status TEXT NOT NULL DEFAULT 'active' CHECK(status IN ('active','suspended','pending')),area TEXT,address TEXT,building TEXT,floor TEXT,apartment TEXT,address_notes TEXT,is_online BOOLEAN NOT NULL DEFAULT false,is_available BOOLEAN NOT NULL DEFAULT false,last_seen_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT now(),updated_at TIMESTAMPTZ NOT NULL DEFAULT now());ALTER TABLE users ADD COLUMN IF NOT EXISTS secondary_phone TEXT;ALTER TABLE users ADD COLUMN IF NOT EXISTS area TEXT;ALTER TABLE users ADD COLUMN IF NOT EXISTS address TEXT;ALTER TABLE users ADD COLUMN IF NOT EXISTS building TEXT;ALTER TABLE users ADD COLUMN IF NOT EXISTS floor TEXT;ALTER TABLE users ADD COLUMN IF NOT EXISTS apartment TEXT;ALTER TABLE users ADD COLUMN IF NOT EXISTS address_notes TEXT;ALTER TABLE users ADD COLUMN IF NOT EXISTS is_online BOOLEAN NOT NULL DEFAULT false;ALTER TABLE users ADD COLUMN IF NOT EXISTS is_available BOOLEAN NOT NULL DEFAULT false;ALTER TABLE users ADD COLUMN IF NOT EXISTS last_seen_at TIMESTAMPTZ;CREATE INDEX IF NOT EXISTS users_role_idx ON users(role);CREATE INDEX IF NOT EXISTS users_status_idx ON users(status);CREATE INDEX IF NOT EXISTS drivers_availability_idx ON users(role,status,is_online,is_available);CREATE TABLE IF NOT EXISTS categories(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),name TEXT NOT NULL,description TEXT,image_url TEXT,sort_order INTEGER NOT NULL DEFAULT 0,is_active BOOLEAN NOT NULL DEFAULT true,created_at TIMESTAMPTZ NOT NULL DEFAULT now(),updated_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS categories_active_order_idx ON categories(is_active,sort_order,created_at);CREATE TABLE IF NOT EXISTS restaurant_profiles(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),restaurant_id UUID NOT NULL UNIQUE REFERENCES users(id) ON DELETE CASCADE,display_name TEXT NOT NULL,description TEXT,logo_url TEXT,cover_url TEXT,contact_phone TEXT,address TEXT,area TEXT,latitude DOUBLE PRECISION,longitude DOUBLE PRECISION,minimum_order NUMERIC(10,2) NOT NULL DEFAULT 0 CHECK(minimum_order>=0),preparation_minutes INTEGER NOT NULL DEFAULT 30 CHECK(preparation_minutes>=0),is_open BOOLEAN NOT NULL DEFAULT true,is_featured BOOLEAN NOT NULL DEFAULT false,created_at TIMESTAMPTZ NOT NULL DEFAULT now(),updated_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS restaurant_profiles_open_idx ON restaurant_profiles(is_open,is_featured,display_name);CREATE TABLE IF NOT EXISTS menu_categories(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),restaurant_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,name TEXT NOT NULL,sort_order INTEGER NOT NULL DEFAULT 0,is_active BOOLEAN NOT NULL DEFAULT true,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS menu_categories_restaurant_idx ON menu_categories(restaurant_id,is_active,sort_order);CREATE TABLE IF NOT EXISTS menu_items(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),restaurant_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,category_id UUID REFERENCES menu_categories(id) ON DELETE SET NULL,name TEXT NOT NULL,description TEXT,price NUMERIC(12,2) NOT NULL CHECK(price>=0),image_url TEXT,is_available BOOLEAN NOT NULL DEFAULT true,sort_order INTEGER NOT NULL DEFAULT 0,created_at TIMESTAMPTZ NOT NULL DEFAULT now(),updated_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS menu_items_restaurant_idx ON menu_items(restaurant_id,is_available,sort_order);CREATE TABLE IF NOT EXISTS platform_settings(key TEXT PRIMARY KEY,value TEXT NOT NULL DEFAULT '',updated_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE IF NOT EXISTS payment_methods(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),code TEXT NOT NULL UNIQUE,name TEXT NOT NULL,description TEXT,account_label TEXT,account_value TEXT,instructions TEXT,requires_proof BOOLEAN NOT NULL DEFAULT false,is_active BOOLEAN NOT NULL DEFAULT true,sort_order INTEGER NOT NULL DEFAULT 0,created_at TIMESTAMPTZ NOT NULL DEFAULT now(),updated_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS payment_methods_active_order_idx ON payment_methods(is_active,sort_order,name);CREATE TABLE IF NOT EXISTS delivery_zones(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),name TEXT NOT NULL,min_latitude DOUBLE PRECISION,max_latitude DOUBLE PRECISION,min_longitude DOUBLE PRECISION,max_longitude DOUBLE PRECISION,fixed_price NUMERIC(10,2),is_active BOOLEAN NOT NULL DEFAULT true,created_at TIMESTAMPTZ NOT NULL DEFAULT now());ALTER TABLE delivery_zones ADD COLUMN IF NOT EXISTS center_latitude DOUBLE PRECISION;ALTER TABLE delivery_zones ADD COLUMN IF NOT EXISTS center_longitude DOUBLE PRECISION;ALTER TABLE delivery_zones ADD COLUMN IF NOT EXISTS radius_meters INTEGER;ALTER TABLE delivery_zones ADD COLUMN IF NOT EXISTS base_fee NUMERIC(10,2);ALTER TABLE delivery_zones ADD COLUMN IF NOT EXISTS included_km NUMERIC(10,2);ALTER TABLE delivery_zones ADD COLUMN IF NOT EXISTS extra_km_fee NUMERIC(10,2);ALTER TABLE delivery_zones ADD COLUMN IF NOT EXISTS extra_merchant_fee NUMERIC(10,2);CREATE INDEX IF NOT EXISTS delivery_zones_active_idx ON delivery_zones(is_active);CREATE TABLE IF NOT EXISTS delivery_distance_rates(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),min_meters INTEGER NOT NULL CHECK(min_meters>=0),max_meters INTEGER,max_price NUMERIC(10,2),price NUMERIC(10,2) NOT NULL CHECK(price>=0),is_active BOOLEAN NOT NULL DEFAULT true,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE TABLE IF NOT EXISTS checkout_sessions(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),customer_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,delivery_latitude DOUBLE PRECISION,delivery_longitude DOUBLE PRECISION,delivery_address TEXT,restaurants_count INTEGER NOT NULL DEFAULT 1,subtotal NUMERIC(12,2) NOT NULL DEFAULT 0,delivery_fee NUMERIC(12,2) NOT NULL DEFAULT 0,total_amount NUMERIC(12,2) NOT NULL DEFAULT 0,payment_method TEXT NOT NULL DEFAULT 'cash',order_notes TEXT,status TEXT NOT NULL DEFAULT 'created' CHECK(status IN ('created','cancelled','completed')),created_at TIMESTAMPTZ NOT NULL DEFAULT now(),updated_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS checkout_sessions_customer_idx ON checkout_sessions(customer_id,created_at DESC);CREATE TABLE IF NOT EXISTS user_locations(user_id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,latitude DOUBLE PRECISION NOT NULL,longitude DOUBLE PRECISION NOT NULL,accuracy DOUBLE PRECISION,heading DOUBLE PRECISION,speed DOUBLE PRECISION,updated_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS user_locations_updated_idx ON user_locations(updated_at DESC);CREATE TABLE IF NOT EXISTS location_history(id BIGSERIAL PRIMARY KEY,user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,latitude DOUBLE PRECISION NOT NULL,longitude DOUBLE PRECISION NOT NULL,accuracy DOUBLE PRECISION,heading DOUBLE PRECISION,speed DOUBLE PRECISION,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS location_history_user_time_idx ON location_history(user_id,created_at DESC);CREATE TABLE IF NOT EXISTS orders(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),customer_id UUID NOT NULL REFERENCES users(id),driver_id UUID REFERENCES users(id),restaurant_id UUID REFERENCES users(id),checkout_id UUID REFERENCES checkout_sessions(id) ON DELETE SET NULL,status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending','restaurant_pending','restaurant_rejected','admin_rejected','confirmed','preparing','ready','assigned','picked_up','on_the_way','delivered','cancelled')),delivery_latitude DOUBLE PRECISION,delivery_longitude DOUBLE PRECISION,delivery_address TEXT,delivery_distance_meters INTEGER,delivery_fee NUMERIC(10,2) NOT NULL DEFAULT 0,subtotal NUMERIC(12,2) NOT NULL DEFAULT 0,total_amount NUMERIC(12,2) NOT NULL DEFAULT 0,payment_method TEXT NOT NULL DEFAULT 'cash',order_notes TEXT,restaurant_rejection_reason TEXT,admin_rejection_reason TEXT,cancelled_by UUID REFERENCES users(id),cancellation_reason TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT now(),updated_at TIMESTAMPTZ NOT NULL DEFAULT now());ALTER TABLE orders ADD COLUMN IF NOT EXISTS checkout_id UUID REFERENCES checkout_sessions(id) ON DELETE SET NULL;ALTER TABLE orders ADD COLUMN IF NOT EXISTS restaurant_rejection_reason TEXT;ALTER TABLE orders ADD COLUMN IF NOT EXISTS admin_rejection_reason TEXT;ALTER TABLE orders ADD COLUMN IF NOT EXISTS cancelled_by UUID REFERENCES users(id);ALTER TABLE orders ADD COLUMN IF NOT EXISTS cancellation_reason TEXT;ALTER TABLE orders ADD COLUMN IF NOT EXISTS delivery_distance_meters INTEGER;ALTER TABLE orders ADD COLUMN IF NOT EXISTS delivery_fee NUMERIC(10,2) NOT NULL DEFAULT 0;ALTER TABLE orders ADD COLUMN IF NOT EXISTS subtotal NUMERIC(12,2) NOT NULL DEFAULT 0;ALTER TABLE orders ADD COLUMN IF NOT EXISTS payment_method TEXT NOT NULL DEFAULT 'cash';ALTER TABLE orders ADD COLUMN IF NOT EXISTS order_notes TEXT;CREATE INDEX IF NOT EXISTS orders_customer_idx ON orders(customer_id,created_at DESC);CREATE INDEX IF NOT EXISTS orders_driver_idx ON orders(driver_id,status,updated_at DESC);CREATE INDEX IF NOT EXISTS orders_dispatch_idx ON orders(driver_id,status,created_at DESC);CREATE INDEX IF NOT EXISTS orders_restaurant_idx ON orders(restaurant_id,status,updated_at DESC);CREATE INDEX IF NOT EXISTS orders_checkout_idx ON orders(checkout_id,created_at);CREATE TABLE IF NOT EXISTS order_items(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),order_id UUID NOT NULL REFERENCES orders(id) ON DELETE CASCADE,menu_item_id UUID REFERENCES menu_items(id) ON DELETE SET NULL,item_name TEXT NOT NULL,unit_price NUMERIC(12,2) NOT NULL CHECK(unit_price>=0),quantity INTEGER NOT NULL CHECK(quantity>0),line_total NUMERIC(12,2) NOT NULL CHECK(line_total>=0),created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS order_items_order_idx ON order_items(order_id);CREATE TABLE IF NOT EXISTS invitations(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),code TEXT NOT NULL UNIQUE,invited_role TEXT NOT NULL CHECK(invited_role IN ('driver','restaurant','staff')),invitee_name TEXT,phone TEXT,email TEXT,restaurant_name TEXT,invited_by UUID NOT NULL REFERENCES users(id),used_by UUID REFERENCES users(id) ON DELETE SET NULL,status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending','used','revoked','expired')),expires_at TIMESTAMPTZ NOT NULL DEFAULT(now()+interval '7 days'),used_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS invitations_status_idx ON invitations(status,expires_at DESC);CREATE INDEX IF NOT EXISTS invitations_phone_idx ON invitations(phone);CREATE TABLE IF NOT EXISTS notifications(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,title TEXT NOT NULL,body TEXT NOT NULL,type TEXT NOT NULL DEFAULT 'general',data JSONB NOT NULL DEFAULT '{}'::jsonb,read_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS notifications_user_idx ON notifications(user_id,read_at,created_at DESC);CREATE TABLE IF NOT EXISTS audit_logs(id BIGSERIAL PRIMARY KEY,actor_id UUID REFERENCES users(id) ON DELETE SET NULL,action TEXT NOT NULL,entity_type TEXT,entity_id UUID,metadata JSONB NOT NULL DEFAULT '{}'::jsonb,created_at TIMESTAMPTZ NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS audit_logs_entity_idx ON audit_logs(entity_type,entity_id,created_at DESC);CREATE INDEX IF NOT EXISTS audit_logs_actor_idx ON audit_logs(actor_id,created_at DESC);`);await pool.query(`INSERT INTO payment_methods(code,name,description,requires_proof,is_active,sort_order) VALUES ('cash','الدفع عند الاستلام','الدفع نقدًا عند وصول الطلب',false,true,0),('wallet','محفظة فالسريع','الدفع من رصيد المحفظة',false,true,1),('vodafone_cash','Vodafone Cash','تحويل إلكتروني مع رفع إثبات التحويل',true,true,2),('instapay','InstaPay','تحويل إلكتروني مع رفع إثبات التحويل',true,true,3) ON CONFLICT(code) DO NOTHING`);await pool.query(`INSERT INTO platform_settings(key,value) VALUES('delivery.base_fee','50'),('delivery.included_km','2'),('delivery.extra_km_fee','15'),('delivery.extra_merchant_fee','15'),('delivery.max_distance_km',''),('delivery.max_merchants',''),('delivery.pricing_mode','distance') ON CONFLICT(key) DO NOTHING`);const adminPhone=String(process.env.PRIMARY_ADMIN_PHONE||'').replace(/[\s-]/g,'');if(adminPhone)await pool.query(`UPDATE users SET role='admin',status='active',updated_at=now() WHERE phone=$1`,[adminPhone]);console.log('Database migration completed')};module.exports=main;if(require.main===module)main().then(()=>pool.end()).catch(async e=>{console.error('Database migration failed:',e);await pool.end();process.exit(1)});
